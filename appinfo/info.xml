<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	  xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
	<id>opencatalogi</id>
	<name lang="en">OpenCatalogi</name>
	<name lang="nl">OpenCatalogi</name>
	<summary lang="en">Federated publication catalogues with DCAT-AP and Woo compliance</summary>
	<summary lang="nl">Gefedereerde publicatiecatalogi met DCAT-AP- en Woo-ondersteuning</summary>
	<description lang="en"><![CDATA[OpenCatalogi turns your Nextcloud into a public-facing catalogue of publications, software, and data. Publish once, get discovered everywhere: entries export as DCAT-AP metadata for data.overheid.nl, and Woo (Wet open overheid) disclosure categories get DIWOO sitemaps and robots.txt out of the box.

**Key Features**

- **Catalogues & publications** — Organize publications into catalogs, each with its own scope and access rules
- **Federated directory** — Synchronize and discover catalogues across Nextcloud instances via the directory sync cron
- **DCAT-AP & DIWOO export** — Publications are harvested as DCAT-AP metadata and mapped to the DIWOO XML schema for all 17 mandatory Woo information categories
- **Woo (open government) compliance** — Sitemap, robots.txt, and audit-trail support for Wet open overheid disclosure obligations
- **Retention lifecycle** — Daily background evaluation of publication retention terms with a review-queue dashboard widget
- **Content management** — Glossary, themes, pages, and menus for building the public-facing catalogue site
- **Organization profiles** — Publisher organizations with government identifiers (OIN/TOOI/RSIN/PKI)
- **AI Chat Companion integration** — MCP tools expose catalogue search and publication lookup to compatible LLM assistants
- 🆓 Free and open source under the EUPL license
- ⚙️ Automatically installs and enables [OpenRegister](https://apps.nextcloud.com/apps/openregister) (required dependency)

**System Cron is currently required for this app to work**

Requirements can be found [here](https://opencatalogi.conduction.nl/docs/Installation/)

Create a [bug report](https://github.com/OpenCatalogi/.github/issues/new/choose)

Create a [feature request](https://github.com/OpenCatalogi/.github/issues/new/choose)

**Support:** For support, contact support@conduction.nl. For a Service Level Agreement (SLA), contact sales@conduction.nl.
    ]]></description>
	<description lang="nl"><![CDATA[OpenCatalogi maakt van je Nextcloud een publieksgerichte catalogus van publicaties, software en data. Eén keer publiceren, overal vindbaar: items worden geëxporteerd als DCAT-AP-metadata voor data.overheid.nl, en Woo-informatiecategorieën krijgen automatisch DIWOO-sitemaps en een robots.txt.

**Belangrijkste functies**

- **Catalogi & publicaties** — Organiseer publicaties in catalogi, elk met een eigen scope en toegangsregels
- **Gefedereerde directory** — Synchroniseer en ontdek catalogi tussen Nextcloud-instanties via de directory-sync cron
- **DCAT-AP- en DIWOO-export** — Publicaties worden geoogst als DCAT-AP-metadata en gemapt naar het DIWOO XML-schema voor alle 17 verplichte Woo-informatiecategorieën
- **Woo-compliance** — Sitemap-, robots.txt- en audittrail-ondersteuning voor de verplichtingen uit de Wet open overheid
- **Bewaartermijnen** — Dagelijkse achtergrondcontrole van bewaartermijnen van publicaties, met een review-queue-widget op het dashboard
- **Contentbeheer** — Woordenlijst, thema's, pagina's en menu's voor het inrichten van de publieke catalogussite
- **Organisatieprofielen** — Uitgevende organisaties met overheidsidentifiers (OIN/TOOI/RSIN/PKI)
- **AI Chat Companion-integratie** — MCP-tools ontsluiten catalogus-zoeken en publicatie-opzoeking voor ondersteunde LLM-assistenten
- 🆓 Vrij en open source onder de EUPL-licentie
- ⚙️ Installeert en activeert automatisch [OpenRegister](https://apps.nextcloud.com/apps/openregister) (vereiste afhankelijkheid)

**System Cron is momenteel vereist om deze app te laten werken**

De vereisten vind je [hier](https://opencatalogi.conduction.nl/docs/Installation/)

Meld een [bug](https://github.com/OpenCatalogi/.github/issues/new/choose)

Doe een [featureverzoek](https://github.com/OpenCatalogi/.github/issues/new/choose)

**Ondersteuning:** Voor ondersteuning, neem contact op via support@conduction.nl. Voor een Service Level Agreement (SLA), neem contact op via sales@conduction.nl.
    ]]></description>
	<version>1.0.9-unstable.20260826232032</version>
	<licence>EUPL-1.2</licence>
	<author mail="info@conduction.nl" homepage="https://www.conduction.nl/">Conduction</author>
	<author mail="info@acato.nl" homepage="https://acato.nl/">Acato</author>
	<namespace>OpenCatalogi</namespace>
    <documentation>
        <user>https://opencatalogi.conduction.nl/docs/Users/</user>
        <admin>https://opencatalogi.conduction.nl/docs/Administrator/</admin>
        <developer>https://opencatalogi.conduction.nl/docs/Developers/</developer>
    </documentation>
    <category>integration</category>
    <website>https://opencatalogi.conduction.nl/</website>
    <!--
        GitHub, not Codeberg. The fleet dropped Forgejo/Codeberg on
        2026-08-19 and GitHub is the only forge again.

        `website` is left alone deliberately — it points at the product
        site, which is still correct and is not a forge link.

        The URLs below all still answered 200 from a stale mirror, which is
        why this lasted: a dead link gets found, a live link to an issue
        tracker nobody reads does not. Each screenshot was checked on the
        new host before being swapped — all three answer 200 image/png from
        raw.githubusercontent on `main`.
    -->
    <bugs>https://github.com/ConductionNL/opencatalogi/issues</bugs>
    <repository type="git">https://github.com/ConductionNL/opencatalogi.git</repository>

    <screenshot>https://raw.githubusercontent.com/ConductionNL/opencatalogi/main/img/screenshot-dashboard.png</screenshot>
    <screenshot>https://raw.githubusercontent.com/ConductionNL/opencatalogi/main/img/screenshot-catalog.png</screenshot>
    <screenshot>https://raw.githubusercontent.com/ConductionNL/opencatalogi/main/img/screenshot-publications.png</screenshot>

   	<dependencies>
        <!--
        min-version is 8.3, not 8.0. The 8.0 floor advertised a configuration
        that cannot exist and that nothing here has ever exercised:

          - composer.json already declares `"php": "^8.3"` and pins
            config.platform.php = 8.3, so `composer install` aborts on anything
            older. The app could not have its own dependencies installed on the
            version its own manifest advertised.
          - The <nextcloud min-version> below is 32, and Nextcloud 32 refuses to
            boot below PHP 8.1 outright (lib/versioncheck.php:
            `if (PHP_VERSION_ID < 80100)`, verified against a running 32.0.12.1).
            8.0 was unreachable through the server, never mind through composer.
          - CI runs `php-version: "8.3"` with `php-test-versions: ["8.3","8.4"]`.
            No leg below 8.3 has ever executed, so 8.0/8.1/8.2 were advertised
            and untested at the same time.

        Same class of defect as openconnector#1173: an App Store listing that
        promises a range the app cannot deliver.
        -->
        <php min-version="8.3" min-int-size="64"/>
        <database min-version="10">pgsql</database>
        <database>sqlite</database>
        <database min-version="8.0">mysql</database>

        <lib>curl</lib>
        <lib>zip</lib>

        <owncloud max-version="0" min-version="0"/>
        <!--
        App dependency (not expressible in app-info.xsd): OpenRegister — every
        catalog, publication and publication-type is an OpenRegister object, and
        src/manifest.json lists "openregister" as a required dependency.

        min-version is therefore 32, NOT 31: openregister declares min-version="32"
        because its lib/ContextChat/ContentProvider.php implements
        OCP\ContextChat\IContentProvider, an interface core does not have before
        Nextcloud 32. On NC 31 openregister refuses to install ("cannot be installed
        because it is not compatible with this version of the server"), so the app
        cannot function there. Declaring 31 advertised a range this app cannot
        deliver. Same defect as openconnector#1173.
        -->
		<nextcloud min-version="32" max-version="34"/>
	</dependencies>

	<background-jobs>
		<job>OCA\OpenCatalogi\BackgroundJob\DirectorySync</job>
		<job>OCA\OpenCatalogi\BackgroundJob\RetentionEvaluation</job>
	</background-jobs>

	<repair-steps>
		<post-migration>
			<step>OCA\OpenCatalogi\Repair\InitializeSettings</step>
			<!--
				Must run AFTER the register sync that adds the English columns, so it
				can tell the rename case from the back-fill case. Idempotent and
				non-destructive; safe to re-run.
			-->
			<step>OCA\OpenCatalogi\Repair\RenameDutchPublicationColumns</step>
			<!--
				The background jobs moved from OCA\OpenCatalogi\Cron to
				OCA\OpenCatalogi\BackgroundJob in #1119. The <job> entries above
				are a REGISTRATION instruction, not a description of state: on
				upgrade Nextcloud ADDS a job it does not have, but never removes
				one whose class disappeared — it cannot tell a renamed class
				from one merely unavailable this boot.

				Measured on a live instance carrying that merge: oc_jobs still
				held OCA\OpenCatalogi\Cron\DirectorySync and
				...\Cron\RetentionEvaluation next to their replacements. A row
				naming a missing class fails to build on every cron tick, and
				logs rather than raises.

				post-migration only: a fresh install has no rows to remove, and
				the step is idempotent either way.
			-->
			<step>OCA\OpenCatalogi\Repair\RemoveRetiredCronJobs</step>
		</post-migration>
		<!--
			Nextcloud runs migrateSchemaOnly() on a FIRST install, so NEITHER
			pre-migration NOR post-migration steps run then — <install> is the only
			unconditional hook (see \OC\Installer::installAppLastSteps). Without this
			block InitializeSettings never ran on a fresh instance, so the OpenCatalogi
			register and schemas were never imported there.

			Only baseline-CREATING steps belong here. RenameDutchPublicationColumns is
			a one-way data migration: on a fresh install there is nothing to rename, so
			it stays post-migration-only.
		-->
		<install>
			<step>OCA\OpenCatalogi\Repair\InitializeSettings</step>
		</install>
	</repair-steps>

	<!-- The app-menu navigation entry is registered in PHP (lib/AppInfo/Application.php::boot),
	     NOT here. The dashboard SPA is served by the engine-namespaced AppHost route
	     OCA\OpenCatalogi\AppHost\Controller\GenericDashboard#page, whose generated route name
	     contains backslashes (opencatalogi.oca\opencatalogi\apphost\controller\genericdashboard.page).
	     A navigations/route entry in this file is validated against the App Store info.xsd
	     route-name pattern [0-9a-zA-Z_]+(\.[0-9a-zA-Z_]+){2}, which rejects that name (HTTP 400
	     on publish). IURLGenerator::linkToRoute() in PHP has no such constraint, so the nav is
	     registered there instead — keeping the app-menu href working AND passing App Store validation. -->

	<settings>
		<admin>OCA\OpenCatalogi\Settings\OpenCatalogiAdmin</admin>
		<admin-section>OCA\OpenCatalogi\Sections\OpenCatalogiAdmin</admin-section>
	</settings>
</info>
