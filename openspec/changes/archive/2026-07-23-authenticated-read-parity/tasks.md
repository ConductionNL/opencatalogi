# Tasks: authenticated-read-parity

- [x] Freeze delta spec `specs/catalogs/spec.md` (ADDED CAT-AUTH-001); `openspec validate authenticated-read-parity` green
  - Done: spec delta was already frozen (shipped with the proposal) at `openspec/changes/authenticated-read-parity/specs/catalogs/spec.md`; confirmed unchanged and still describes the shipped behaviour. `openspec validate authenticated-read-parity` reports "Change 'authenticated-read-parity' is valid".
- [x] Extract the `$unwantedProperties` strip list to a single private constant; audit `PublicationService`/`PublicationQueryService`/controllers for duplicate strip lists and route them through it
  - Spec ref: CAT-AUTH-001
  - Acceptance: grep shows exactly one definition of the strip list in lib/
  - Done: extracted to `CatalogiService::UNWANTED_SELF_PROPERTIES` (`lib/Service/CatalogiService.php`) — the class that originally carried the `@todo`. `PublicationService::filterUnwantedProperties()` now reads `CatalogiService::UNWANTED_SELF_PROPERTIES` (same `OCA\OpenCatalogi\Service` namespace, no new DI needed for a class constant). Audited `PublicationQueryService.php` and every controller under `lib/Controller/` (`grep -rn "'schemaVersion'\|'retention'\|unwantedProperties\|array_diff_key" lib/`) — no duplicate list found anywhere else; `grep -rn "'schemaVersion',\s*$" lib/` now matches exactly one array literal (the constant). Deviation: the constant is `public const`, not `private const` — PHP private class constants are invisible outside their declaring class, so a genuinely private constant cannot be "referenced everywhere" across `CatalogiService` and `PublicationService` (two sibling classes, no inheritance relation). `public const` is the visibility that satisfies "single source of truth, referenced by every assembler" while still being a compile-time constant (no extra constructor coupling).
- [x] Inject `IUserSession` into `CatalogiService` (+ confirmed siblings); strip only when `getUser() === null`
  - Spec ref: CAT-AUTH-001
  - Acceptance: PHPUnit: authenticated envelope carries owner/locked/retention; anonymous envelope byte-identical to golden fixture
  - Done: `?IUserSession $userSession=null` added as the last constructor param on both `CatalogiService` and `PublicationService` (nullable/optional, fail-closed to anonymous when null — matches the existing `PublicationQueryService::isAnonymous()` convention). `CatalogiService::index()` and `PublicationService::filterUnwantedProperties()` compute `$isAnonymous = ($this->userSession === null || $this->userSession->getUser() === null)` once per request and only strip when true. New tests: `CatalogiServiceTest::testIndexAnonymousEnvelopeIsByteIdenticalToGoldenFixture` / `::testIndexAuthenticatedEnvelopeCarriesFullMetadata`; `PublicationServiceTest::testFilterUnwantedPropertiesAnonymousEnvelopeIsByteIdenticalToGoldenFixture` / `::testFilterUnwantedPropertiesAuthenticatedEnvelopeCarriesFullMetadata` — all green.
- [x] Object-set parity test: same ids/order for both audiences under identical mocked RBAC context
  - Spec ref: CAT-AUTH-001 scenario 3
  - Done: `CatalogiServiceTest::testIndexObjectSetParityBetweenAnonymousAndAuthenticated` and `PublicationServiceTest::testFilterUnwantedPropertiesObjectSetParityBetweenAudiences` — same mocked `searchObjectsPaginated()`/object list fed to both an anonymous-session and an authenticated-session service instance; assert identical `['obj-a', 'obj-b']` id order for both, while only the authenticated run carries `owner` in `@self`.
- [x] Remove the `@todo` comment at the strip site; `@spec` tags on changed methods; hydra gates green locally (spec-coverage, security-change-has-tests — tests/ touched in same PR)
  - Done: `@todo: a logged-in user should be able to see the full object.` removed from `CatalogiService::index()` (the session-awareness it asked for is now implemented). `@spec openspec/changes/authenticated-read-parity/specs/catalogs/spec.md` added to: `CatalogiService` class docblock, `CatalogiService::__construct()`, `CatalogiService::index()` (alongside its existing `catalogs/spec.md` tag), `PublicationService` class docblock, `PublicationService::__construct()`, `PublicationService::filterUnwantedProperties()` (alongside its existing `publications/spec.md` tag). Both files keep their pre-existing `@license EUPL-1.2` / `@copyright` / `SPDX-License-Identifier` header tags unmodified. `spec-coverage` gate: every constructor/method touched now carries the tag. `security-change-has-tests`: this is a session-dependent response-shaping change and ships with 6 new PHPUnit tests in the same PR (3 per service — anonymous golden-fixture, authenticated full-metadata, object-set parity). PHPCS on the two changed `lib/` files: 0 errors (was 1 pre-existing-adjacent error introduced transiently by the edit, fixed by adding a missing `//end if` comment); 2 pre-existing warnings remain per file (`getAvailableRegisters()`/`getAvailableSchemas()` missing `@spec` — confirmed present on the base commit too, unrelated getters this change does not touch, left alone to keep the diff scoped). PHPUnit: `223 → 229` tests in the targeted filter (`CatalogiServiceTest|PublicationServiceTest|PublicationQueryServiceTest`), all green; full suite `1221 → 1227`, same 3 pre-existing unrelated failures as the base commit (`SetupControllerTest` x2, `DirectoryServiceTest` x1), zero new failures.
