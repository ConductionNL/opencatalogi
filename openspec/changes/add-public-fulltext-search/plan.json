{
  "change": "add-public-fulltext-search",
  "project": "opencatalogi",
  "repo": "Conduction/opencatalogi",
  "base_branch": "development",
  "feature_branch": "feat/impl-public-fulltext-search",
  "created": "2026-07-03",
  "tracking_issue": 84,
  "tasks": [
    {
      "id": 1,
      "title": "Freeze the delta spec under `openspec/changes/add-public-fulltext-search/specs/search/spec.md` (MODIFIED SCH-OR-003 + ADDED SCH-PFTS-001 \u2026 SCH-PFTS-007); confirm `openspec validate add-public-fulltext-search` is green *(done in this PR \u2014 re-run `openspec validate` after any post-review spec tweaks before implementation begins)*",
      "status": "done",
      "spec_ref": "specs/search/spec.md (this change)",
      "acceptance_criteria": [
        "openspec validator reports \"is valid\"; SCH-OR-001/SCH-OR-002 untouched; SCH-OR-003 carried through the MODIFIED block"
      ]
    },
    {
      "id": 2,
      "title": "Add the `document` schema to `lib/Settings/publication_register.json` under `components.schemas` (alongside `publication`, `catalog`, `page`, `menu`, `theme`, `glossary`, `listing`, `organization`, `usageCounter`)",
      "status": "pending",
      "spec_ref": "SCH-PFTS-005",
      "acceptance_criteria": [
        "schema present under `components.schemas.document`; `searchable: true`; `authorization.read` mirrors `publication`'s anonymous projection; magic mapper auto-allocates `oc_openregister_table_publication_document` on first install (no `configuration.schemas` block needed \u2014 the bundle's existing schemas rely on the same auto-allocation)"
      ]
    },
    {
      "id": 3,
      "title": "Add seed data per ADR-001 + design.md \"Seed Data\": 2 seed publications (municipality + consultancy scenarios) AND 2 seed documents linking to them. The current bundle ships NO seed publications, so this step adds both surfaces together. Each seed row MUST be self-identifying as demo data \u2014 `example-` slug prefix, `Voorbeeld:` title prefix, and a `**Voorbeeld / seed data**` banner opening the description.",
      "status": "pending",
      "spec_ref": "ADR-001 (Seed Data) + design.md \"Seed Data\"",
      "acceptance_criteria": [
        "4 new entries under `components.objects[]` with the bundle's canonical `@self: { register, schema, slug }` envelope (no top-level `id`); each document row carries an embedded `publication: { slug, title }` referencing one of the two new seed publications; every seed slug starts with `example-`, every seed title starts with `Voorbeeld:`, and every seed description opens with `**Voorbeeld / seed data**`; re-import is idempotent (matches on `@self.slug`); no real Dutch municipality names used (Gemeente Voorbeeld is fictional)"
      ]
    },
    {
      "id": 4,
      "title": "Promote `lib/Controller/SearchController::index()` to public \u2014 remove the anonymous-401 guard, annotate with `#[PublicPage]` + `#[NoCSRFRequired]`",
      "status": "pending",
      "spec_ref": "SCH-PFTS-001",
      "acceptance_criteria": [
        "anonymous GET returns HTTP 200 with a result envelope; no path to HTTP 401 on the endpoint"
      ]
    },
    {
      "id": 5,
      "title": "Confirm the existing `GET /apps/opencatalogi/api/search` route entry in `appinfo/routes.php` is retained and its handler carries `#[PublicPage]` + `#[NoCSRFRequired]`; no new route path is added (SCH-PFTS-001 explicitly forbids introducing a new endpoint path for this purpose)",
      "status": "pending",
      "spec_ref": "SCH-PFTS-001",
      "acceptance_criteria": [
        "existing route reused (no new entry added); `gate-route-reachability` and `gate-route-auth` both green"
      ]
    },
    {
      "id": 6,
      "title": "Add a new method on `PublicationQueryService` (e.g. `assemblePublicSearchResults()`) that calls OR's `zoeken-filteren` across both `publication` and `document` schemas and merges candidate rows into one flat array; do NOT filter the OR-side search surface down to a subset of properties. Placement is decided (see design.md \"Service placement decision\") \u2014 MUST live in `PublicationQueryService` so it can co-locate with the `isObjectPublic()` helper it reuses; no sibling service.",
      "status": "pending",
      "spec_ref": "SCH-PFTS-002, SCH-PFTS-006, SCH-PFTS-007",
      "acceptance_criteria": [
        "response envelope is a single array; each row carries `@self.schema`; no separate `publications`/`documents` sub-arrays; matches surface across every `searchable` property of both schemas (verified by a test that adds a new string property to one of the seed rows and confirms it matches without controller changes)"
      ]
    },
    {
      "id": 7,
      "title": "Embed `publication: {id, slug, title}` (English `title` \u2014 matches the bundled publication schema) on each document row during assembly",
      "status": "pending",
      "spec_ref": "SCH-PFTS-003",
      "acceptance_criteria": [
        "every document row in the response carries the embedded publication summary with English field names; rows whose linked publication is missing are dropped. **Note the seed-vs-response asymmetry** \u2014 seed rows in `publication_register.json` carry `publication.{slug, title}` (no `id`, because the magic mapper assigns UUIDs at import time; see design.md \"Seed publications\"); API-response rows MUST carry `publication.{id, slug, title}`. A diligent implementer testing the seed shape should not expect `id` to be present in the seed JSON."
      ]
    },
    {
      "id": 8,
      "title": "Apply `isObjectPublic()` to anonymous results AFTER scoring/merge (post-filter, never folded into the OR query)",
      "status": "pending",
      "spec_ref": "SCH-PFTS-004",
      "acceptance_criteria": [
        "PHPUnit test demonstrates that a candidate row excluded by visibility was present pre-filter and absent post-filter; ordering invariant documented in the helper's docblock"
      ]
    },
    {
      "id": 9,
      "title": "Enforce transitive visibility on documents: drop document rows whose linked publication fails `isObjectPublic()`",
      "status": "pending",
      "spec_ref": "SCH-PFTS-004",
      "acceptance_criteria": [
        "PHPUnit test with a document whose linked publication is depublished; document MUST NOT surface for anonymous callers"
      ]
    },
    {
      "id": 10,
      "title": "Add Newman / PHPUnit tests proving anonymous reachability + 200 status on `/apps/opencatalogi/api/search`",
      "status": "pending",
      "spec_ref": "SCH-PFTS-001",
      "acceptance_criteria": [
        "tests run in CI; cover the no-auth-header and depublished-row cases"
      ]
    },
    {
      "id": 11,
      "title": "Add regression coverage proving `GET /publications` is unchanged (existing assertions still pass; no new fields, no removed fields, no admission-rule drift)",
      "status": "pending",
      "spec_ref": "SCH-PFTS-001 (negative)",
      "acceptance_criteria": [
        "existing publications endpoint tests run unmodified; no diff in their fixtures"
      ]
    },
    {
      "id": 12,
      "title": "Document Path A vs Path B in the controller / service docblock (linking to design.md's dual-path section) so future implementers know which extraction surface to wire when Ruben's answer arrives",
      "status": "pending",
      "spec_ref": "design.md \"Dual-path design\"",
      "acceptance_criteria": [
        "docblock comment references the proposal's \"Pending decisions\" block; no extraction code added in this change"
      ]
    },
    {
      "id": 13,
      "title": "Update `openspec/specs/search/spec.md` (the canonical capability) to list this change under an `**OpenSpec changes**` block with status `in-progress` *(done in this PR)*",
      "status": "done",
      "spec_ref": null,
      "acceptance_criteria": [
        "spec carries a canonical `**Status**: in-progress / **Scope**: opencatalogi / **OpenSpec changes**:` block listing `add-public-fulltext-search`"
      ]
    },
    {
      "id": 14,
      "title": "Add a `CHANGELOG.md` entry noting the shape change on `GET /apps/opencatalogi/api/search`: (a) mixed publication + document rows discriminated by `@self.schema`, (b) anonymous reachability replacing the prior HTTP 401 posture, (c) the prior admin-only response shape is removed. Cross-reference WOO-506 and mention that any lingering admin consumer of the old shape needs to switch.",
      "status": "pending",
      "spec_ref": "proposal.md \"Risks\" (admin backwards-compat break)",
      "acceptance_criteria": [
        "`CHANGELOG.md` carries a dated entry under the next release section; WOO-506 stakeholders in the ticket are pinged with a link to the entry"
      ]
    },
    {
      "id": 15,
      "title": "Run the Hydra mechanical gates locally (`scripts/run-hydra-gates.sh --scope-to-diff`) and resolve every BLOCKING issue before opening the PR",
      "status": "pending",
      "spec_ref": null,
      "acceptance_criteria": [
        "all 45 gates either PASS or carry a documented exclusion"
      ]
    },
    {
      "id": 16,
      "title": "Open the PR; confirm reviewer + security-reviewer pass; address inline review nits without scope creep",
      "status": "pending",
      "spec_ref": null,
      "acceptance_criteria": [
        "PR review verdict is APPROVE on both lanes"
      ]
    },
    {
      "id": 17,
      "title": "Confirm Ruben's answer received + (if needed) open a follow-up OpenSpec change before archiving this one",
      "status": "pending",
      "spec_ref": null,
      "acceptance_criteria": [
        "Ruben's decision on document content indexing is recorded in WOO-506; if \"no/later\", a B3 change (e.g. `add-document-content-search`) is created and linked from this change's proposal before archive"
      ]
    }
  ]
}
