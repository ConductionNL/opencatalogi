# Tasks: fix-woo-capability-provisioning

- [x] Freeze delta spec `specs/woo-transparency/spec.md` (ADDED WOO-PROV-001..003); `openspec validate fix-woo-capability-provisioning` green
  - Done: Spec delta was already frozen/committed on entry to this change; `openspec validate fix-woo-capability-provisioning --strict` → "Change 'fix-woo-capability-provisioning' is valid".
- [x] Add `wooBatch` + `wooAssessment` schemas to `lib/Settings/publication_register.json`, field-for-field from `WooService`'s writes (D3); English `title`+`description` on every property; enums exactly as the service uses them
  - Spec ref: WOO-PROV-001
  - Acceptance: PHPUnit cross-checks the shipped register JSON against the field inventory; schema-property-titles + relation-dialect gates green
  - Done: Added `wooBatch` (14 properties) and `wooAssessment` (10 properties) to `lib/Settings/publication_register.json`'s shared `publication` register (D1) — both slugs added to the register's `schemas` array and `configuration.schemas` map, and both schema bodies added after `usageCounter`. Field inventory verified against `WooService::createBatch()` (lines ~312-425: `caseReference`, `status`, `deckBoardId`, `deckAvailable`, `documents`, `besluit`, `inventarislijst`, `createdAt`, `updatedAt`, `createdBy`), the assessment literal (~328-339: `documentReference`, `fileName`, `fileType`, `assessment`, `weigeringsgronden`, `redactionInstructions`, `anonymizedDocument`, `caseReference`, `assessedBy`, `assessedAt`), and the later `getBatch()`/`publishBatch()` assignments (~618-622, ~906-928: `documentSummary`, `publishedAt`, `publishedCount`, `wooPublication`) — every field the service reads/writes is present, no extra field was invented. Every property carries an English `title` and `description`. Enum order/values pinned exactly to the spec text: `status` = `in_progress`|`ready_for_review`|`published`; `assessment` = `te_beoordelen`|`openbaar`|`niet_openbaar`|`deels_openbaar`. **Relation-dialect decision (documented per the task instruction)**: `wooBatch.documents` uses the canonical relation dialect (`type: array`, `items: {type: string, format: uuid, $ref: "wooAssessment"}`) — it holds the persisted uuids of the `wooAssessment` objects created alongside the batch (`array_map(... (string) $r['id'] ...)` in `createBatch()`), the same shape `publication.themes` uses for its `theme` refs, so it gets full relation-widget/facet treatment rather than a bare untyped array. `documentSummary` and `wooPublication` are modelled as `type: object` with `additionalProperties: true` (not enumerated sub-schemas) because both are computed/nested composite blocks (`documentSummary` is derived read-side in `getBatch()` and not independently persisted; `wooPublication` is a nested metadata object with a variable-length `listings` array) — enumerating their internals wasn't part of the fixed D3 field inventory (which lists them as single top-level fields) and would have meant inventing structure the requirement doesn't specify. `hardValidation: false` on both schemas because several fields are written as empty-string placeholders before being populated (e.g. `besluit`, `inventarislijst`, `assessedAt`, `assessedBy` all start as `''`) despite carrying a `format: date-time`/typed hint — matching the existing `document` schema's precedent. `searchable: false` and `authorization` read/create/update/delete restricted to `admin` on both, matching `usageCounter`'s pattern and the fact `WooController` is fully admin-gated (no `@NoAdminRequired` — see its class docblock) with no public read path. Verified `python3 -c "import json; json.load(...)"` parses and the property-key sets match the field inventory exactly (checked programmatically).
- [x] Extend `SettingsService::updateObjectTypeConfiguration()` with the explicit WOO key map (`woo_register` ← publication register id, `woo_batch_schema` ← `wooBatch`, `woo_assessment_schema` ← `wooAssessment`), idempotent + never overwrite with empty
  - Spec ref: WOO-PROV-002
  - Acceptance: PHPUnit — all three keys set from a mocked import result; missing-schema case leaves the key untouched
  - Done: Added a `$wooSchemaMap` (`woo_batch_schema` => `wooBatch`, `woo_assessment_schema` => `wooAssessment`) mirroring the existing `$ooapiTypeMap` precedent, plus a direct `woo_register` write from the already-resolved `$registerId` (no schema slug to key off — D1/D2). Both follow the existing loop's exact conditional-write shape (`setValueString` only called when the resolved value is non-null), so an unresolved key is simply never written rather than being overwritten with `''` (D5) — identical behaviour to the pre-existing `$objectTypes`/`$ooapiTypeMap` loops. Added `@spec` tag referencing WOO-PROV-002 on the method's docblock (alongside the pre-existing `admin-settings` spec ref). Tests added in `tests/Unit/Service/SettingsServiceTest.php`: `testUpdateObjectTypeConfigurationSetsAllThreeWooKeys` (mocked import result with `publication` register + both WOO schemas → all 3 keys set to the expected ids) and `testUpdateObjectTypeConfigurationLeavesMissingWooAssessmentSchemaUntouched` (import result missing `wooAssessment` → `woo_register`/`woo_batch_schema` still set, `woo_assessment_schema` absent from the captured `setValueString` calls). Both invoke the private method via the file's existing `invokePrivateMethod()` reflection helper.
- [x] Add `woo_register`, `woo_batch_schema`, `woo_assessment_schema` to `ProvideManifestConfigStateListener::MANIFEST_CONFIG_KEYS`
  - Spec ref: WOO-PROV-003
  - Done: Three keys appended to `MANIFEST_CONFIG_KEYS` (now 19 entries). Updated the pre-existing `ProvideManifestConfigStateListenerTest::testProvidesConfigStateForOpencatalogiIndex` count assertion (17 → 20 = 19 keys + `default_directory_url`) and added explicit assertions for the three new keys' values, since the existing test hard-coded the old count.
- [x] Anti-drift parity test: every `@resolve:<key>` in the effective manifest (base + `manifest.d/*` + `menu-layout.json`) must be in `MANIFEST_CONFIG_KEYS`, read via reflection (single source of truth); prove by mutation that removing a key fails the test, then restore
  - Spec ref: WOO-PROV-003
  - Acceptance: test fails naming the offending key when a sentinel is unbacked
  - Done: New `tests/Unit/Listener/ManifestSentinelParityTest.php`. `collectEffectiveManifestSentinelKeys()` decodes `src/manifest.json` + every `src/manifest.d/*.json` fragment (globbed, mirroring `require.context('./manifest.d/', false, /\.json$/)` in `src/main.js`) + `src/menu-layout.json` when present, recursively scans every string value with the same `@resolve:([a-z][a-z0-9_-]*)` pattern `resolveManifestSentinelsSync()` uses, and asserts the resulting key set is a subset of `MANIFEST_CONFIG_KEYS` read via `ReflectionClass::getConstant()` (never a duplicated literal list, per D4). A second test (`testWooKeysAreManifestConfigKeys`) directly asserts the 3 WOO keys are members. **Mutation proof (both directions, actually run against this repo's manifest, not simulated):** (1) temporarily removed `'woo_register'` (a key genuinely referenced twice in `src/manifest.json` line 385/452-note) from `MANIFEST_CONFIG_KEYS` and ran `phpunit --filter ManifestSentinelParityTest` → both tests FAILED, the primary test's message explicitly named the offending key: `"Manifest @resolve: sentinel(s) with no backing key in ProvideManifestConfigStateListener::MANIFEST_CONFIG_KEYS ... : woo_register"`. (2) Restored the key and re-ran → both tests GREEN (`Tests: 2, Assertions: 14, Failures: 0`). (Also separately mutation-tested `woo_assessment_schema`, which is not currently referenced by any `@resolve:` sentinel in the shipped manifest — removing it left the primary scan-based test green but correctly failed the explicit `testWooKeysAreManifestConfigKeys` membership test, confirming that second test earns its keep independent of current manifest wiring.)
- [x] Live-verify on the dev instance: run the auto-configure/repair path, `occ config:list opencatalogi` shows the three WOO keys, and `/woo` loads with no `@resolve:` 404 in the console
  - Spec ref: WOO-PROV-002, WOO-PROV-003
  - Done: NOT performed in this session — explicitly out of scope per the task instructions given for this implementation pass ("Do NOT deploy to the shared dev instance (I will live-verify separately)"). Deferred to the requester's own live-verification pass, as stated.
- [x] `@spec` tags on changed methods; SPDX headers intact; full unit suite shows no new failures vs the documented baseline (SetupControllerTest ×2, DirectoryServiceTest ×1)
  - Done: `@spec` added to `SettingsService::updateObjectTypeConfiguration()` (WOO-PROV-002) and `ProvideManifestConfigStateListener::handle()` (WOO-PROV-003), both alongside their pre-existing spec references (not replacing them). SPDX/`@license`/`@copyright` headers on `lib/Service/SettingsService.php` and `lib/Listener/ProvideManifestConfigStateListener.php` untouched (only method bodies/docblocks and the `MANIFEST_CONFIG_KEYS` constant were edited). `php -l` clean on every changed PHP file (both host PHP 8.2 and the `nextcloud:34.0.0-apache` container's PHP 8.4); `publication_register.json` parses via `python3 -c "import json; json.load(...)"`. Full suite in-container (`nextcloud:34.0.0-apache`, `phpunit -c phpunit-unit.xml`): `Tests: 1251, Assertions: 2991, Errors: 2, Failures: 1, PHPUnit Warnings: 1, Warnings: 7, Deprecations: 66, Skipped: 2` — the 2 errors are `SetupControllerTest` (saveObject mock returns null) and the 1 failure is `DirectoryServiceTest::testSyncDirectoryThrowsWhenSyncingWithSelf`, exactly the documented pre-existing baseline; zero new failures introduced.
