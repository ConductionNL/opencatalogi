# PHPStan baseline — tracked lint debt for opencatalogi.
#
# Empty: legacy debt fully retired (opencatalogi-legacy-quality-cleanup Task 10).
# The Guzzle RequestException edge case in DirectoryService::syncDirectory was
# fixed by constructing a synthetic Psr7\Request from the upstream directoryUrl
# rather than passing null.
parameters:
	ignoreErrors: []
