# Tasks: add-document-content-search

This change is `kind: mixed` (per ADR-032). Ships the OpenCatalogi-side wiring only — depended on the OR-side prerequisite (`_content_search` flag on `ObjectService::searchObjectsPaginated()`) landing first. **That prerequisite has shipped** — merged to `Conduction/openregister:development` at commit `e9d4d787` (PR #473) — so impl proceeds.

- [x] Freeze the delta spec under `openspec/changes/add-document-content-search/specs/search/spec.md` (MODIFIED SCH-PFTS-002 + ADDED SCH-PFTS-CONTENT-001 / -002 / -003); confirm `openspec validate add-document-content-search` is green
  - Spec ref: specs/search/spec.md (this change)
  - Acceptance: openspec validator reports "is valid"; SCH-PFTS-001 / -003 / -004 / -005 / -006 / -007 untouched
  - Done: spec delta was already frozen (shipped with the proposal); confirmed unchanged and still describes the shipped behaviour.
- [x] Verify OR-side prerequisite has shipped in `Conduction/openregister:development`: `ObjectService::searchObjectsPaginated()` accepts a `_content_search` (or agreed final name) flag and routes to `ChunkMapper::searchByKeyword()` when set; document the exact flag name + return shape in this task before impl starts
  - Spec ref: (external) openregister/openspec/changes/expose-content-search-in-object-service/
  - Acceptance: OR release notes or merge commit link recorded here; smoke curl against a seeded OR install returns chunk-matched document rows
  - Done: Merged — `Conduction/openregister:development` commit `e9d4d787` ("Merge pull request 'feat(search): expose `_content_search` on searchObjectsPaginated (WOO-517, closes #472)' (#473)"). Flag name: `_content_search` (boolean), read from `$query['_content_search']` inside `ObjectService::searchObjectsPaginated()`'s `$query` array (no method-signature change — see OR design.md Decision D1). Wired via `OCA\OpenRegister\Service\Object\ContentSearchHandler::augmentWithChunkMatches()`, invoked from `QueryHandler::searchObjectsPaginatedDatabase()`. Return shape: the SAME `searchObjectsPaginated()` envelope (`results`/`total`) — OR already resolves each chunk hit to its owning `ObjectEntity` and appends it to `results` (never a raw chunk), deduplicated on OR's own `getId()` before returning (see OR design.md Decision D4). The `/srv/nextcloud/apps/openregister` copy pre-installed in the build container is a stale mirror pinned to an older commit; this does not block OC-side impl since the flag is forwarded as a plain query-array key with no OR method-signature dependency.
- [x] Extend `PublicationQueryService::assemblePublicSearchResults()` to accept a `_content` boolean flag from `$queryParams` (default false) and forward the corresponding OR flag when true
  - Spec ref: SCH-PFTS-CONTENT-001
  - Acceptance: PHPUnit test with `_content=false` (default) proves current behaviour is byte-identical to WOO-506; separate test with `_content=true` proves the OR flag is set on the delegated query
  - Done: `lib/Service/PublicationQueryService.php` — `$contentSearchRequested` read via `filter_var(..., FILTER_VALIDATE_BOOLEAN)` (matches the codebase's existing boolean-query-param convention); `_content_search` forwarded on the OR query only when true; OC's own `_content` key is stripped before forwarding. Covered by `testAssemblePublicSearchResultsOmitsContentSearchFlagByDefault`, `...OmitsContentSearchFlagWhenExplicitlyFalse`, `...ForwardsContentSearchFlagWhenTrue`.
- [x] When `_content=true`, execute two searches under the same query scope and deduplicate on `@self.id`: metadata-search (existing WOO-506 path) UNION content-search (chunks routed via the OR flag). Preserve the WOO-506 flat envelope; each document row is indistinguishable in shape whether it matched on metadata or content
  - Spec ref: SCH-PFTS-CONTENT-002, MODIFIED SCH-PFTS-002
  - Acceptance: a document that matches BOTH via title AND body text appears exactly once in the response; a document that matches ONLY via body text is present when `_content=true` and absent when `_content=false`
  - Done: single delegated `searchObjectsPaginated()` call (OR internally unions metadata-match + chunk-match per its own design D2); OC-side `@self.id` dedup added as an additional guarantee at the assembly layer. Covered by `testAssemblePublicSearchResultsContentMatchedDocumentRowShapeMatchesMetadataMatch`, `...DedupesDocumentMatchingBothSurfaces`.
- [x] Apply the existing `isObjectPublic()` filter + transitive publication-visibility gate (SCH-PFTS-004) to content-matched document rows before returning
  - Spec ref: SCH-PFTS-CONTENT-003
  - Acceptance: PHPUnit test proves a body-text match on a depublished document (`depublicatiedatum` in the past) is dropped from the anonymous response; separate test proves a body-text match on a document whose linked publication is depublished is also dropped
  - Done: no new filter needed — content-matched rows flow through the SAME per-row loop (schema discrimination, publication-summary resolution, `isObjectPublic()`) as metadata-matched rows; there is no separate code path to bypass the gate. Covered by `testAssemblePublicSearchResultsDropsContentMatchedDepublishedDocument`, `...DropsContentMatchedDocumentWithDepublishedParent`.
- [x] E2E test: seed a publication + a document with an attached PDF (or DOCX) carrying a distinctive phrase like `"lorem-ipsum-woo517-marker"`; poll the OR extraction job until chunks are indexed; assert `GET /apps/opencatalogi/api/search?_search=lorem-ipsum-woo517-marker&_content=true` returns the document row with correct `@self.schema=document` + embedded publication summary
  - Spec ref: SCH-PFTS-CONTENT-001, -002, -003
  - Acceptance: E2E green on both fresh MariaDB and fresh PostgreSQL dev envs (MariaDB may return unranked results — see deferred MariaDB-parity note in proposal.md)
  - Done: `tests/e2e/spec-coverage/content-search-endpoint.spec.ts` (`@e2e search::content-search-surfaces-a-body-text-only-match`) — seeds a publication + linked document, attaches a plain-text file (simpler than a PDF/DOCX binary fixture; OR's extractor treats `text/*` identically to the other formats per proposal.md's extractor-stack list) carrying a distinctive per-run marker phrase, force-triggers extraction via `POST /apps/openregister/api/files/{id}/extract` (avoids waiting on the lazy cron job), then asserts the marker is absent from the anonymous response without `_content` and present with `_content=true`. Not executed in this build container (no live NC+OR stack with WebDAV/cron here — sandbox only has an isolated SQLite instance for PHPUnit); written to the same pattern as the existing `tests/e2e/workflows/*` fixtures and validated via `npx playwright test --list` + `tsc --noEmit` + `eslint`. Runs for real in the CI e2e stage.
- [ ] Update `docs/Integrations/fulltext-search.md` on `Conduction/openwoo-app-website` (twin PRs against `development` and `documentation` branches, matching the WOO-506 docs-branch pattern) — add a section describing the `_content=true` opt-in on Endpoint 2, its default OFF, and the fact that content matches share the same envelope + visibility rules as metadata matches
  - Spec ref: SCH-PFTS-CONTENT-001, -002, -003
  - Acceptance: both openwoo PRs open, both diffs identical
  - Status: NOT DONE — out of this container's reach (separate `Conduction/openwoo-app-website` repo, not cloned in this build; this repo's PR does not depend on it merging). Flagged as a follow-up in the PR description for a human/subsequent docs pass.
- [ ] Confirm archive gate: after impl + E2E green + docs merged, run `openspec archive add-document-content-search`
  - Spec ref: —
  - Acceptance: change moved under `openspec/changes/archive/YYYY-MM-DD-add-document-content-search`
  - Status: NOT DONE — blocked on the openwoo-app-website docs task above merging first.
