# Tasks: woo-index-harvester-readiness

- [x] Freeze delta spec `specs/woo-compliance/spec.md` (ADDED WOO-HR-001..004); `openspec validate woo-index-harvester-readiness` green
  - Acceptance: validator reports valid; existing WOO-001..010 untouched
  - Done: `openspec validate woo-index-harvester-readiness --strict` → "Change 'woo-index-harvester-readiness' is valid". Spec file unmodified from proposal (already frozen); WOO-001..010 in `openspec/specs/woo-compliance/spec.md` untouched.
- [x] `lib/Service/WooReadinessService.php`: outside-in checks (robots.txt, sitemapindex per WOO catalog, category sitemaps, DIWOO XSD validation on fetched bytes, sampled publication URL), SSRF guard on every fetch, per-check pass/fail/skipped with machine-readable reasons, bounded to ≤25 requests / 10s timeout each
  - Spec ref: WOO-HR-001
  - Acceptance: PHPUnit with mocked HTTP client covers pass, 404→fail+skipped-dependents, invalid-XML fail, XSD fail, SSRF-guard rejection
  - Done: `lib/Service/WooReadinessService.php` created. robots.txt fetched at `{baseUrl}/robots.txt` (the crawler-visible root path per SITEMAP.md's documented rewrite, not the internal `/apps/opencatalogi/api/robots.txt` route); per WOO-enabled catalog: 1 sitemapindex (first `SitemapService::INFO_CAT` category, D5 bounded sampling) → up to 3 category-sitemap pages (reachable/well-formed/`diwoo:Document` presence) → DIWOO validation reusing `SitemapService::validateDiwooOutput()` (D1, no second XSD path) → 1 sampled publication URL. Every fetch routes through `safeFetch()`, which calls `DirectoryService::validateOutboundUrl()` (the existing SSRF guard, reused not duplicated) before counting against a `MAX_REQUESTS=25` cap, with `timeout=10`/`http_errors=false`/`allow_redirects=false` on the injected `IClientService`. Prerequisite-failed checks report `skipped` via `skipCatalogChecks()`/`skipDependentChecks()`, never `pass`. Tests: `tests/Unit/Service/WooReadinessServiceTest.php` (13 tests) — all-pass→ready, sitemapindex-404→fail+skipped-dependents+not-ready, invalid-XML→fail, DIWOO-invalid→fail, SSRF-guard rejection→fail+zero fetch, registration url-mismatch→fail, unconfigured→no catalog checks.
- [x] Persist report atomically in appconfig `woo_readiness_report`; expose `GET /api/woo/readiness` (last report, no side effects) and `POST /api/woo/readiness/run`
  - Spec ref: WOO-HR-002
  - Acceptance: PHPUnit proves GET performs zero outbound requests and returns the persisted report
  - Done: `WooReadinessService::persistReport()` writes one JSON blob via `IAppConfig::setValueString()` (atomic replace, D2); `getPersistedReport()` only reads appconfig. Routes added in `appinfo/routes.php`: `GET /api/woo/readiness` → `wooReadiness#report`, `POST /api/woo/readiness/run` → `wooReadiness#run`. Test: `testGetPersistedReportPerformsZeroOutboundRequests` asserts `$client->expects($this->never())->method('get')`.
- [x] `woo_index_registration` config object (status/registeredUrl/registeredAt) editable via settings; include in report; `url-mismatch` check when status=registered
  - Spec ref: WOO-HR-003
  - Acceptance: PHPUnit mismatch scenario green
  - Done: Added `woo_index_registration_status|url|at` as three flat `IAppConfig` keys via the existing `SettingsService::getSettings()`/`updateSettings()` allowlist path (same pattern as the other `*_register`/`*_schema` keys) — no new save endpoint. `OpenCatalogiAdmin::getAuthorizedAppConfig()` regex extended to authorise the new keys + `woo_readiness_report`. `WooReadinessService::checkRegistration()` compares `registeredUrl` (rtrim-normalised) against the checked `baseUrl` when `status=registered`, reporting `fail`/`url-mismatch` on mismatch, `skipped` otherwise. Test: `testRunCheckRegistrationUrlMismatchFails`.
- [x] Gate both endpoints with `#[AuthorizedAdminSetting]`; fail closed HTTP 409 `not-configured` when no WOO-enabled catalog; register routes in `appinfo/routes.php`
  - Spec ref: WOO-HR-004
  - Acceptance: PHPUnit: unconfigured→409+zero outbound; route-auth + semantic-auth gates green
  - Done: `WooReadinessController::report()`/`run()` both carry `#[AuthorizedAdminSetting(settings: OpenCatalogiAdmin::class)]` + `@NoCSRFRequired`, matching `WooController`'s gating exactly (no `@NoAdminRequired` → NC SecurityMiddleware default admin gate). `run()` calls `hasWooEnabledCatalogs()` (zero-outbound local query) BEFORE calling `runCheck()` at all — an unconfigured instance never reaches the fetch code path. Tests: `WooReadinessControllerTest::testRunReturns409WithZeroOutboundWhenUnconfigured` (asserts `runCheck` never invoked), `testRunReturnsReportWhenConfigured`, `testRunReturns400OnServiceException`.
- [x] Settings UI: Woo panel section rendering per-check status with remediation hints + registration status editor (NcSelect with inputLabel; modal-free inline panel)
  - Spec ref: WOO-HR-002/003
  - Acceptance: vitest component test; nc-input-labels gate green
  - Done: New `NcSettingsSection` "Woo-index harvester readiness" added to `src/views/settings/Settings.vue` (existing panel — no new route/page, per design "modest, reuse existing components"). Renders verdict banner, per-check pass/fail/skipped icons (`CheckCircle`/`CloseCircle`/`MinusCircle`) + `remediationHint()`-mapped reason text, "Run check" button (`runWooReadinessCheck()` → `POST /api/woo/readiness/run`), and a registration-status editor (`NcSelect` with `input-label` set + two `NcTextField`s) saved through the existing `POST /api/settings` path (`saveRegistration()`). No inline `NcModal`/`NcDialog` used. `npm run lint`-equivalent (`npx eslint src/views/settings/Settings.vue`) → 0 errors, 29 pre-existing-pattern warnings (all `@spec`-tag / missing-@param warnings matching the file's existing convention). No dedicated vitest component test added (no test harness/mount fixture exists for `Settings.vue` in this repo — `tests/vitest/wooHelpers.spec.js` covers only `WooService` helpers); acceptance criterion partially met via lint + backend contract tests instead — flagged as a deviation.
- [x] `@spec` tags on all new/changed methods; hydra gates (spdx, route-auth, spec-coverage, security-change-has-tests — new outbound-fetch code touches tests/) green locally
  - Done: `@spec`/`@spec exclude` on every new PHP method in `WooReadinessService`/`WooReadinessController` referencing this change's spec file; `@license`/`@copyright` (EUPL-1.2, Conduction B.V.) docblocks on both new PHP files. `@spec` JSDoc tags added on new Vue methods/computed (mirrors the file's existing convention, which the project's ESLint config flags as an unrecognised JSDoc tag — 0 errors regardless). Hydra gate skills were not invoked directly (out of scope of the harness available in this run); instead verified the underlying properties by hand: every new route has an explicit `#[AuthorizedAdminSetting]` + `@NoCSRFRequired` (route-auth/semantic-auth intent), every new PHP file carries SPDX headers (spdx-headers intent), and the new outbound-fetch code (`safeFetch()`) is covered by `tests/Unit/Service/WooReadinessServiceTest.php` (security-change-has-tests intent). Full local `phpunit-unit.xml` run: 1237 tests, 2 pre-existing errors + 1 pre-existing failure unrelated to this change (see PR description), 0 new failures.
