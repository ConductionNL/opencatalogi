## 1. Scaffolding + dependency wiring

- [ ] 1.1 Create `lib/Service/FulltextSearchService.php` skeleton — class declaration, constructor injection (`ObjectService $objectService`, `LoggerInterface $logger`), stubs for `isEngineEngaged(array $query): bool` and `search(array $query, Catalog $catalog, ObjectService $objectService): array`. Reference: design D1. **Files**: `lib/Service/FulltextSearchService.php` (new). **Acceptance**: class auto-loads, instantiable via DI, public surface matches design D1.
- [ ] 1.2 Add `FulltextSearchService` to the OpenCatalogi DI container in `lib/AppInfo/Application.php` (or wherever services are registered). **Files**: `lib/AppInfo/Application.php`. **Acceptance**: `\OC::$server->get(FulltextSearchService::class)` returns an instance without error.
- [ ] 1.3 Deduplication check per ADR-001 — grep `openspec/specs/` and `openregister/lib/Service/` for prior FTS / parser / scorer code. Document findings (expected: no overlap). **Files**: capture findings in a comment inside `FulltextSearchService.php` ("Reuse analysis — no overlap found, see design.md Reuse Analysis section"). **Acceptance**: comment present, no shipped code duplicates existing service.

## 2. Parser (depends on §1)

- [ ] 2.1 Implement the single-pass tokenizer state machine — char-by-char loop with OUTSIDE_QUOTE / INSIDE_QUOTE states, emitting `term` / `phrase` / `wildcard` raw tokens. Spec_ref: `publications-fts-engine#FTS-001` to `FTS-005`; design D2. **Files**: `lib/Service/FulltextSearchService.php` (private method `tokenize(string $input): array`). **Acceptance**: every input in design D2's "Operator capitalisation" + FTS-005's "mixed-case operator is treated as a term" tokenises deterministically; unclosed quotes are recovered as a single phrase token (D9).
- [ ] 2.2 Implement AST builder — walks the flat token list, recognises uppercase `OR` / `AND` operators (strict ASCII string compare), emits `orBranches: Term[][]` two-level structure. Spec_ref: FTS-001, FTS-002, FTS-005. **Files**: `lib/Service/FulltextSearchService.php` (private method `parse(array $tokens): array`). **Acceptance**: every FTS-001 to FTS-005 scenario produces the expected AST shape (verified in §10 unit tests).
- [ ] 2.3 Enforce 8-branch hard cap on OR branches (D9 DoS mitigation) — branches beyond index 8 are dropped with a WARN-level log entry. **Files**: `lib/Service/FulltextSearchService.php`. **Acceptance**: a 10-branch input `_search=a OR b OR c OR d OR e OR f OR g OR h OR i OR j` parses to exactly 8 branches; one log entry recorded.

## 3. `searchConfig` schema-block resolution (depends on §1)

- [ ] 3.1 Implement `searchConfig` reader — given a `Schema` object, returns a `[fieldname => weight]` map. Default `1.0` for any string property not listed. Spec_ref: FTS-006, FTS-007; design D6. **Files**: `lib/Service/FulltextSearchService.php` (private method `resolveFieldWeights(Schema $schema): array`). **Acceptance**: a schema with `x-openregister.searchConfig.fields: {titel: {weight: 5}}` + 4 other string properties returns `{titel: 5, summary: 1, description: 1, ...}`; a schema with no searchConfig returns `{<every string field>: 1}`.
- [ ] 3.2 Validate orphan / mistyped fields — `searchConfig.fields` keys that don't match a schema property OR refer to a non-`type:string` property are logged at WARN level and dropped. Spec_ref: design D6. **Files**: same as 3.1. **Acceptance**: schema with `searchConfig.fields: {nonexistent: {weight: 5}, numericField: {weight: 3}}` produces 2 log warnings; `nonexistent` and `numericField` excluded from the returned weight map.

## 4. Sub-query + result merge (depends on §2 + §3)

- [ ] 4.1 Implement per-OR-branch sub-query issuance — for each branch in the AST, build a clone of the request query envelope with `_search` replaced by the branch's content, call `$objectService->searchObjectsPaginated($subQuery, _rbac: true, _multitenancy: false)`. Spec_ref: FTS-010; design D3. **Files**: `lib/Service/FulltextSearchService.php` (private method `executeBranches(array $ast, array $baseQuery): array`). **Acceptance**: every sub-query passes `_rbac: true`; sequential issuance (no parallelism in B2).
- [ ] 4.2 Implement result merge + de-dup by `@self.uuid` — collect rows from all branches, build a map keyed by uuid, record branch-provenance per row. Spec_ref: FTS-010; design D4. **Files**: `lib/Service/FulltextSearchService.php` (private method `mergeBranches(array $branchResults): array`). **Acceptance**: a row matching 3 OR-branches appears once in the output; its `provenance` field lists `[0, 1, 2]`.
- [ ] 4.3 Implement implicit-AND post-filter — for each row, for each OR-branch it was matched by, verify EVERY term in that branch appears via `stripos` in some indexed field. Rows failing this check are dropped from that branch's contribution. Spec_ref: FTS-002; design D4. **Files**: `lib/Service/FulltextSearchService.php` (private method `enforceImplicitAnd(array $rows, array $ast, array $weights): array`). **Acceptance**: query `_search=verzoek vergunning` against a row containing only `verzoek` (no `vergunning`) is dropped; row with both is retained.

## 5. Scorer (depends on §3 + §4)

- [ ] 5.1 Implement per-row raw score — Σ (term-occurrences-in-field × field_weight) over every indexed string field per the formula in design D5. **Files**: `lib/Service/FulltextSearchService.php` (private method `scoreRow(array $row, array $ast, array $weights): float`). **Acceptance**: a row with `titel`=`Convenant` (weight 5) and a single match returns `5.0` × 1 = 5.0; a row with the same term in `samenvatting` (weight 3) returns 3.0.
- [ ] 5.2 Implement per-result-set IDF penalty — for each token, count how many rows in the merged set contain it; penalty = `1 / log(1 + corpus_freq_ratio)`. Spec_ref: FTS-008; design D5. **Files**: `lib/Service/FulltextSearchService.php` (private method `applyIdfPenalty(array $rowScores, array $tokenCounts, int $totalRows): array`). **Acceptance**: a token appearing in all rows gets a penalty ≈ `1/log(2)`; a token in 1 of 10 rows gets penalty ≈ 1.0.
- [ ] 5.3 Implement final normalisation to 0–100 integer — divide each row's adjusted raw score by `max(raw_scores)` × 100, cast to `int` via `round()`. Empty result set returns empty array. Spec_ref: FTS-008. **Files**: `lib/Service/FulltextSearchService.php` (private method `normaliseToHundred(array $adjustedScores): array`). **Acceptance**: the top-ranked row has `relevance = 100`; all other rows have `relevance < 100` (strict-less when scores differ); zero-row input returns empty array.
- [ ] 5.4 Inject `relevance` into each row's `@self` envelope — only when the engine ran (i.e., `_search` was present). Field MUST be absent when `_search` was absent (FTS-008 negative scenario). **Files**: `lib/Service/FulltextSearchService.php` (private method `attachRelevance(array $rows, array $scores): array`). **Acceptance**: every result row's `@self.relevance` is an integer in [0, 100]; a parallel run without `_search` returns rows whose `@self` lacks the `relevance` key entirely.

## 6. Solr bypass (depends on §1)

- [ ] 6.1 Implement Solr bypass in `isEngineEngaged($query)` — at the top of the method, check `$this->objectService->isSolrAvailable()`. If true, return `false` (engine yields). Spec_ref: FTS-011; design D7. **Files**: `lib/Service/FulltextSearchService.php`. **Acceptance**: when `isSolrAvailable()` returns true, `isEngineEngaged()` returns false even with operators in `_search`. Verified by mocking `ObjectService` in §10.

## 7. Sort by `@self.relevance` (depends on §5)

- [ ] 7.1 Sort merged result-set by `@self.relevance desc` when `_order[@self.relevance]=desc` is present in the query (also support `=asc`). Spec_ref: FTS-009. **Files**: `lib/Service/FulltextSearchService.php` (private method `applySort(array $rows, array $query): array`). **Acceptance**: query with `_order[@self.relevance]=desc` produces rows in descending relevance order; with `=asc` ascending. Default (no `_order`) preserves the `@self.published desc` default from the existing path (FTS-012).
- [ ] 7.2 Reject `_order[@self.relevance]` without `_search` — return HTTP 400 with a descriptive error from `PublicationsController::index` (the controller's existing validation layer, not the engine). Spec_ref: FTS-009 negative scenario. **Files**: `lib/Controller/PublicationsController.php` (early-validation block in `index()`). **Acceptance**: `GET /api/publications?_order[@self.relevance]=desc` (no `_search`) returns 400 with body containing the phrase `_search is required for relevance ordering`.

## 8. Integration seam in `PublicationQueryService` (depends on §1–§7)

- [ ] 8.1 Add `FulltextSearchService` to `PublicationQueryService` constructor — DI-inject and store as private readonly property. **Files**: `lib/Service/PublicationQueryService.php`. **Acceptance**: existing PublicationQueryService unit tests still pass with the new constructor parameter (use container resolution in tests).
- [ ] 8.2 Add the engine-engagement branch in `buildCatalogSearchQuery()` — single early `if ($this->ftsService->isEngineEngaged($query)) { return $this->ftsService->search($query, $catalog, $objectService); }`. Spec_ref: PUB-FTS-001; design D8. **Files**: `lib/Service/PublicationQueryService.php`. **Acceptance**: query with `_search=foo OR bar` goes through the engine path; query without `_search` goes through the legacy path; query with `_search=plain-term` and no operators ALSO goes through the engine path BUT (per FTS-012) returns the same row-set as the legacy path.

## 9. Anonymous-filter ordering + over-fetch (depends on §8)

- [ ] 9.1 Verify `PublicationsController::index` calls `isObjectPublic()` AFTER the engine's merged + scored result-set is returned. Spec_ref: PUB-FTS-004. **Files**: `lib/Controller/PublicationsController.php`. **Acceptance**: a draft publication (no `@self.published`) that the engine matches against `_search` is filtered out anonymously; its `@self.relevance` is never visible to anonymous callers.
- [ ] 9.2 Implement 1.5× over-fetch in `FulltextSearchService::search()` — internally request `ceil($limit * 1.5)` candidates per OR-branch so that anonymous post-filter rejects don't create pagination gaps. Spec_ref: design D8, Risk 4. **Files**: `lib/Service/FulltextSearchService.php`. **Acceptance**: a query with `_limit=10` against a register where 3 of the top-15 matches are drafts returns exactly 10 published rows (not 7).

## 10. Unit tests — parser + scorer + searchConfig + Solr bypass

- [ ] 10.1 Parser tests — one PHPUnit test method per FTS-001 to FTS-005 scenario from the spec. **Files**: `tests/Unit/Service/FulltextSearchServiceParserTest.php` (new). **Acceptance**: all 5+ scenarios from the spec's parser-related requirements pass. Run via `composer phpunit -- --filter Parser`.
- [ ] 10.2 Scorer tests — synthetic inputs producing known relevance ranks: single-term in single-field, same term in multi-field (weight sum), repeated term in single field (count × weight), high-freq token IDF penalty, empty set, single-row set (must return 100). Spec_ref: FTS-006 to FTS-009. **Files**: `tests/Unit/Service/FulltextSearchServiceScorerTest.php` (new). **Acceptance**: all 6+ scoring scenarios pass; relevance ranks are deterministic.
- [ ] 10.3 `searchConfig` resolver tests — schema with full config, schema with partial config (some properties default to 1.0), schema with orphan/typed-wrong entries (warnings + drops), schema with no config block at all. Spec_ref: FTS-006, FTS-007. **Files**: `tests/Unit/Service/FulltextSearchServiceConfigTest.php` (new). **Acceptance**: all 4+ scenarios pass.
- [ ] 10.4 Solr-bypass test — mock `ObjectService::isSolrAvailable()` to return true; verify `isEngineEngaged()` returns false and `search()` is never reached. Spec_ref: FTS-011. **Files**: `tests/Unit/Service/FulltextSearchServiceSolrBypassTest.php` (new). **Acceptance**: one test method demonstrating bypass; one demonstrating engine engagement when Solr is not available.
- [ ] 10.5 Run full unit test suite — `composer phpunit` — confirm all green. **Acceptance**: 100% pass on the new test files; no regressions in pre-existing tests.

## 11. Seed data per ADR-001 (depends on nothing — can be done in parallel)

- [ ] 11.1 Add `x-openregister.searchConfig.fields` block to the `publication` schema in `lib/Settings/publication_register.json` with weights per design's Seed Data section (`title: 5, summary: 3, description: 2`; other string fields get default `1.0` via FTS-007). Spec_ref: FTS-006; design Seed Data. **Files**: `lib/Settings/publication_register.json`. **Acceptance**: re-running `SettingsService::autoConfigure()` on a fresh install loads the schema with the searchConfig block present.
- [ ] 11.2 Add two seed publications matching the design's Seed Data table — `seed-municipality-convenant` (Den Haag) and `seed-consultancy-rapport` (Conduction). Use the `@self` envelope with `slug` matching the keys for idempotent re-import. Spec_ref: ADR-001 Seed data; design Seed Data. **Files**: `lib/Settings/publication_register.json` (under `components.objects[]`). **Acceptance**: importing the register on a clean install creates exactly two new publications; re-importing is a no-op (matched by slug per ADR-001 idempotency).
- [ ] 11.3 Verify the bundled `publication` schema's actual property names match what `searchConfig` lists. If the bundled schema uses different field names than the design assumed (`title` vs `titel`), update `searchConfig.fields` keys to match. **Files**: `lib/Settings/publication_register.json`. **Acceptance**: `grep -E '"title":|"titel":' lib/Settings/publication_register.json` returns the correct property name; `searchConfig.fields` keys align with it.

## 12. Integration tests against the local NC dev stack (depends on §1–§11)

- [ ] 12.1 Set up the integration-test catalog — wire register 13 (or whichever the dev env exposes) to the `publications` catalog and ensure both new seed publications are present. **Files**: `tests/Integration/FulltextSearchIntegrationTest.php` (new — uses `setUp()` to issue REST calls to the running NC dev stack). **Acceptance**: the test's `setUp()` confirms the two seed publications exist anonymously; if not, it skips (so unit-test-only environments don't fail).
- [ ] 12.2 End-to-end test — every FTS-NNN scenario from the spec executed against the running stack: boolean OR returns union, implicit AND returns intersection, phrase returns adjacent-only, wildcard returns prefix-matches, operator case-sensitivity, `@self.relevance` present with `_search`, absent without, `_order[@self.relevance]=desc` reorders. Spec_ref: every FTS-001 to FTS-012. **Files**: `tests/Integration/FulltextSearchIntegrationTest.php`. **Acceptance**: 12+ test methods, each named after a FTS-NNN requirement.
- [ ] 12.3 Backwards-compat regression test — execute a vanilla apiv2-shaped query (`_search=verzoek&_order[@self.published]=desc&_limit=20`) BEFORE the engine is enabled (feature flag off) and AFTER (flag on); assert row IDs + ordering are identical. Spec_ref: FTS-012. **Files**: same as 12.1. **Acceptance**: one test method asserting result-set equality; failure produces a diff of row UUIDs.
- [ ] 12.4 PUB-FTS-004 anonymous-filter ordering test — seed a draft publication that matches `_search=convenant`; anonymous request MUST omit it and MUST NOT leak `@self.relevance` for the draft. **Files**: same as 12.1. **Acceptance**: response body's `results[]` does not contain the draft's UUID; nowhere in the response does the draft's relevance appear.

## 13. Newman/Postman API tests (depends on §11 + §12)

- [ ] 13.1 Add new Postman collection items under the existing publications-API folder — one request per representative query shape (plain term, boolean OR, phrase, wildcard, `_fuzzy=true`, `_order[@self.relevance]=desc`). **Files**: `docs/assets/Opencatalogi CRUD.postman_collection.json` (extend existing collection). **Acceptance**: `newman run` against the running NC dev stack passes for each new request; pre-existing requests still pass.
- [ ] 13.2 Add response-body assertion scripts to each new Postman item — verify status 200, expected row count (where deterministic), presence of `@self.relevance` when applicable. **Files**: same. **Acceptance**: every new collection item has at least one `pm.test()` assertion; `newman run --verbose` shows pass count = collection-item count.

## 14. Documentation + spec traceability

- [ ] 14.1 Add `@spec openspec/changes/add-publications-fts-engine/tasks.md#task-N` PHPDoc tags to every new/touched method in `FulltextSearchService` and `PublicationQueryService::buildCatalogSearchQuery` per hydra-gate-spec-coverage requirement. **Files**: every file touched in §1–§9. **Acceptance**: `scripts/run-hydra-gates.sh` gate-16 (spec-coverage) passes on the change's diff.
- [ ] 14.2 Update `openspec/specs/publications-fts-engine/spec.md` (will be created by `/opsx-sync` on archive) to reflect any in-flight learning — e.g., parser corner-cases discovered during §10 testing. Mark deviations from the delta spec with `(Deviation: …)` notes. **Files**: handled at archive-time; this task is a checkpoint to confirm no spec changes are pending. **Acceptance**: parser test outcomes match the spec's scenarios; if any scenario was revised, the spec file is updated before archive.
- [ ] 14.3 Cross-repo follow-up note — once this change merges + canary picks it up, update `openwoo-app-website/docs/Integrations/fulltext-search.md` to remove the "out-of-scope" framing on B2 features (boolean OR/AND, phrase, wildcards, scoring) and document them as supported. Open a tracking note in the openwoo-app-website repo (NOT a Codeberg issue per user request — just a TODO comment in the doc itself). **Files**: cross-repo (`openwoo-app-website/docs/Integrations/fulltext-search.md`). **Acceptance**: a TODO comment is added to that doc referencing this change; full doc rewrite happens after canary upgrade.

## 15. Verify all hydra-gates pass on the diff

- [ ] 15.1 Run `scripts/run-hydra-gates.sh --scope-to-diff` against the change branch — confirm all relevant gates green (SPDX, route-auth, IDOR, spec-coverage, contract-coverage, redundant-controller, etc.). **Files**: full diff. **Acceptance**: gate exit code 0; any WARN-level findings documented in the PR body.
- [ ] 15.2 Run `composer phpcs` + `npm run lint` (if any frontend changes — should be none for this API-only change). **Acceptance**: lint passes.
- [ ] 15.3 Run `composer phpunit` (full suite, not just new tests) — confirm no regressions in pre-existing tests. **Acceptance**: pass count matches pre-change baseline + the new test count.
