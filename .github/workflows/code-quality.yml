name: Code Quality

on:
  push:
    branches: [main, beta, development, feature/**, bugfix/**, hotfix/**]
  pull_request:
    types: [opened, reopened]
    branches: [main, beta, development]
  workflow_dispatch:

concurrency:
  group: quality-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

# NO top-level `permissions:` block, deliberately.
#
# A reusable workflow's jobs can never hold MORE permission than the calling
# job. `quality.yml` declares `contents: write` (coverage-ratchet / features
# extract commit back) and `actions: write`; a caller pinned to
# `contents: read, packages: read` therefore makes the whole call
# unsatisfiable and GitHub aborts the run with `startup_failure` and ZERO
# jobs -- indistinguishable at a glance from the unresolvable-org fault.
# This is why `main`, whose org reference was already correct, was ALSO
# producing zero jobs. Matches larpingapp / openbuild / planix, which declare
# no caller-side permissions and run the full matrix green.

jobs:
  quality:
    if: github.event_name != 'push' || github.event.created != true
    uses: ConductionNL/.github/.github/workflows/quality.yml@main
    with:
      app-name: opencatalogi
      php-version: "8.3"
      php-test-versions: '["8.3", "8.4"]'
      nextcloud-test-refs: '["stable32"]'
      enable-psalm: true
      enable-phpstan: true
      enable-phpmetrics: true
      enable-frontend: true
      enable-eslint: true
      # PHPUnit re-enabled after the unit-suite revival. The original CI hang was
      # caused by the DirectoryService::syncDirectory / BroadcastService::broadcast
      # tests driving the SSRF outbound-URL guard, which does real DNS resolution
      # (dns_get_record / gethostbyname) and stalls on runners without outbound DNS.
      # Those tests are now tagged @group network and excluded by default in both
      # phpunit.xml and phpunit-unit.xml, so the suite is deterministic and offline-safe.
      enable-phpunit: true
      additional-apps: '[{"repo":"ConductionNL/openregister","app":"openregister","ref":"development"}]'
      enable-newman: false
